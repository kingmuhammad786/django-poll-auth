"""
Admin configuration for the User Authentication application.

Registers models to enable management via Django's admin panel.
"""

from django.contrib import admin
from .models import UserProfile  # Replace with actual models when added

# Register models for admin management
# Uncomment and modify when models are added
# admin.site.register(UserProfile)
