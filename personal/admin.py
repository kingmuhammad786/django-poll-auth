"""
Admin configuration for the Personal application.

This file registers models in Django's admin panel, allowing content
to be managed through the admin interface.
"""

from django.contrib import admin
from .models import PersonalModel  # Replace 'PersonalModel' with actual models when needed

# Register models for admin management
# Uncomment and modify when models are added
# admin.site.register(PersonalModel)

